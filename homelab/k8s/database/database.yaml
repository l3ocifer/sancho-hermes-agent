---
# Sancho's Postgres database — declarative replacement for the
# `CREATE DATABASE hermes_sancho` line that used to live in
# homelab-pg.yaml's postInitSQL (which only runs once at cluster
# bootstrap, so it was useless for adding/recreating the agent later).
#
# CNPG handles idempotency: re-applying just no-ops if the DB already
# exists with the right owner.
#
# The `sancho` role MUST exist before this resource reconciles —
# bootstrapped by role-bootstrap-job.yaml (sync-wave 0).
apiVersion: postgresql.cnpg.io/v1
kind: Database
metadata:
  name: hermes-sancho
  namespace: databases
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  cluster:
    name: homelab-pg
  name: hermes_sancho
  owner: sancho
  ensure: present
  encoding: UTF8
  databaseReclaimPolicy: retain
